<div class="contents row" >
    <p><%= @nickname %>さんの投稿一覧</p>
    <% @items.each do |item| %>
       <%=render partial:"items/item",locals:{item: item} %>
    <% end %>
    <%= paginate(@items) %>
  </div>