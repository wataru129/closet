<div class="contents row">
    <%= form_tag("/items/#{@item.id}", method: :patch ) do %>
      <h3>
        編集する
      </h3>
      <input placeholder="Image Url" type="text" name="image" value="<%= @item.image %>" autofocus="true">
      <textarea cols="30" name="text" placeholder="description" rows="10"><%= @item.description %></textarea>
      <input type="submit" value="SENT">
    <% end %>
  </div>